import React from 'react'

const HotSubCategory = (props) => {
  return (
<div>
    <span>{props.categoryName}</span>
    <span>{props.categoryNumber}</span>    
</div>
  )
}

export default HotSubCategory